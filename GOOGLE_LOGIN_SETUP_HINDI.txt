HimRideG — Google Login Setup (APP CODE(5) Full Replace)

IMPORTANT:
- Ye full project APP CODE(5).zip se bana hai.
- Original files/data preserve kiye gaye hain.
- Sirf Google Login ke required canonical files change/add kiye gaye hain.
- Root duplicate source files intentionally untouched hain.
- Existing OTP routes/code delete nahi kiya gaya; fallback safe rakha hai.
- Admin login Email + Password hi rahega.
- Customer + Driver me Google Login add hua hai.

REAL ENV:
1) client/.env me existing VITE_API_URL aur VITE_SOCKET_URL preserve hain.
   Isme VITE_GOOGLE_CLIENT_ID= line add ki gayi hai.
   Google Cloud se Web Client ID milne ke baad us line me value daalni hai.

2) Original APP CODE(5).zip me server/.env file thi hi nahi.
   Isliye fake server/.env create nahi ki gayi aur koi secret invent nahi kiya gaya.
   Agar aapke laptop me server/.env already hai to use delete/overwrite MAT karna.
   Us existing file me ye key add karna:
   GOOGLE_CLIENT_ID=<same Web Client ID>

3) Production Render backend ke render.yaml me GOOGLE_CLIENT_ID sync:false add hai.
   Render Dashboard -> Backend service -> Environment me GOOGLE_CLIENT_ID set karna hoga.

4) Frontend Render static site ke Environment me:
   VITE_GOOGLE_CLIENT_ID=<same Web Client ID>
   add karke redeploy karna hoga.

Google Cloud Authorized JavaScript Origins me normally:
- https://himrideg.com
- https://www.himrideg.com
- local testing ke liye http://localhost:5173

No Google Client Secret frontend me nahi dalna hai.
